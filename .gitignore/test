state("minit")
{
	int reset : "minit.exe", 0x161898, 0x0;
}

init
{
    Action resetVars = () =>
    {
        vars.reset = 4;
    };
    vars.resetVars = resetVars;
    vars.resetVars();
}

split
{
	if (current.reset != old.reset)
	{
		return true;
	}
}
